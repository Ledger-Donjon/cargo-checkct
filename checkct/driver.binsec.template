load sections .text, .rodata from file
starting from <__checkct>
with concrete stack pointer
replace <__checkct_private_rand>{thumb} () by
    res<{size}> := secret
    return res
end
replace <__checkct_public_rand>{thumb} () by
    res<{size}> := nondet
    return res
end
halt at <__check_ct_exit>{thumb}
explore all
